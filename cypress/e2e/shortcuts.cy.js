describe("Shortcuts", () => {
    beforeEach(() => {
        cy.visit("/");
    });

    it("should accordingly to each shortcut", () => {
        // write some text with markings
        // apply a suggestion
        // focus to a marking
        // apply another suggestion
        // focus to a marking
        // Escape
        // d
        // c
        // h
    });
});
